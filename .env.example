# ============================================================
# OpsIQ Environment Configuration
# Copy this to .env and fill in values as needed
# ============================================================

# --- LLM (Groq = FREE & FAST, recommended) ---
# Step 1: Go to https://console.groq.com → Sign up (free, no credit card)
# Step 2: API Keys → Create API Key
# Step 3: Paste below
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# --- LLM Fallback (OpenAI, optional) ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# --- Server ---
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# --- Frontend -> Backend API URL (optional for deployment) ---
# Local default is http://localhost:8000 if this is unset.
BACKEND_URL=
