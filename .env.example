# ============================================================
# OpsIQ Environment Configuration
# Copy this to .env and fill in values as needed
# ============================================================

# --- Mode ---
# "mock" = demo without real sponsor API keys (data from CSV)
# "real" = use real Datadog/Lightdash/Airia APIs
OPSIQ_MODE=mock

# --- LLM (Groq = FREE & FAST, recommended) ---
# Step 1: Go to https://console.groq.com → Sign up (free, no credit card)
# Step 2: API Keys → Create API Key
# Step 3: Paste below
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# --- LLM Fallback (OpenAI, optional) ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# --- Datadog ---
# https://app.datadoghq.com → Organization Settings → API Keys
DATADOG_API_KEY=
DATADOG_APP_KEY=
DATADOG_SITE=datadoghq.com

# --- Lightdash ---
# https://app.lightdash.cloud → Settings → Personal Access Tokens
LIGHTDASH_URL=
LIGHTDASH_API_KEY=
LIGHTDASH_PROJECT_UUID=

# --- Airia ---
# https://airia.ai → Agent Studio → Settings → Interfaces → API Key
AIRIA_API_URL=https://api.airia.ai
AIRIA_API_KEY=

# --- Modulate (ToxMod) ---
# https://console.modulate.ai → Developer Resources → API Key
MODULATE_API_KEY=
MODULATE_API_URL=https://api.modulate.ai

# --- Server ---
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
